[project]
name = "robot-data-hub"
version = "0.1.0"
description = "Robot dataset selection and download tools"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub>=1.0,<2.0",
    "httpx[socks]>=0.28.1,<1.0",
]
